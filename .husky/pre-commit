#!/bin/sh
# Check yarn.lock files for unresolved merge conflicts
if grep -qE '<<<<<<<|=======|>>>>>>>' yarn.lock frontend/yarn.lock; then
  echo "❌ Commit blocked: yarn.lock files have merge conflict markers."
  exit 1
fi
